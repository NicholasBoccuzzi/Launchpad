import merge from 'lodash/merge';

const rewardReducer = (state = {}, action) => {
  Object.freeze(state);
  switch (action.type) {

    default:
      return state;
  }
};

export default rewardReducer;
